# Retrofit, OkHttp, Room, Hilt, WorkManager and kotlinx.serialization publish
# consumer R8 rules. Keep app rules intentionally minimal and add a rule only
# when a release build or mapping inspection demonstrates a concrete need.
